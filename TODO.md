# TODO: Modify "Read More" Button to Open in New Tab

## Steps to Complete:
- [x] Edit `frontend/src/components/NewsCard.jsx` to change the "Read More" button's onClick handler from `window.location.href` to `window.open` with '_blank' to open in a new tab.
- [ ] Test the change by running the frontend and verifying the button opens the article in a new tab.
